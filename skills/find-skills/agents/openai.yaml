interface:
  display_name: "Find Skills"
  short_description: "Find and install relevant agent skills"
  default_prompt: "Use $find-skills to check the local catalog first, then find the right skill if none exists."
