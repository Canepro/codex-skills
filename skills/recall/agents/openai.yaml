interface:
  display_name: "Recall"
  short_description: "Rebuild recent working context without transcripts"
  default_prompt: "Use $recall to rebuild my recent working context on this topic from shared memory, git and gh state, and the shared record, then give me the current-state brief."
