interface:
  display_name: "Frontend Anti-Slop"
  short_description: "Redesign generic UI into product-specific frontend"
  default_prompt: "Use $frontend-uncodixfy to turn frontend-review findings into a cleaner, product-specific UI and verify the rendered result."
